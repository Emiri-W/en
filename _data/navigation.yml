header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /index.html

  - titles:
      # @start locale config
      en      : &EN       CV
      # @end locale config
    url: /cv.html

  - titles:
      # @start locale config
      en      : &EN       News
      # @end locale config
    url: /news.html

  - titles:
      # @start locale config
      en      : &EN       Contact
      # @end locale config
    url: /contact.html
    
  - titles:
      # @start locale config
      en      : &EN       日本語
      # @end locale config
    url: https://emiri-w.github.io/